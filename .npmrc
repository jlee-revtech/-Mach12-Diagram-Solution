# @jlee-revtech/agent-core (the shared Super Consultant core) is hosted in GitHub
# Packages. npm install (locally AND in the Vercel build) needs NODE_AUTH_TOKEN set
# to a token with read:packages.
@jlee-revtech:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
